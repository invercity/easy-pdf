extends condition
block content
    style.
        .h2-title {
            padding: 12px 0 10px 0;
            margin: 0;
        }

        .cols {
            display: table;
            width: 100%;
        }

        .pdf-column {
            display: table-cell;
            margin: 0;
            padding: 0;
            margin-bottom: 20px;
        }

        .pdf-list {
            margin: 0;
            padding: 0;
            list-style: none;
            display: table;
            width: 100%;
        }

        .pdf-list-header {
            padding: 6px 0 0 0;
        }

        .li {
            background: #FFFFFF;
            padding: 6px 0;
        }

        .li:nth-child(even) {
            background: #f1f1f1;
        }
    if (title)
        h2(class='h2-title', style=title.style) #{title.value}
    if (description)
        h2 #{description}
    .cols(style={"font-size": options.fontSize})
        each item in names ? names: []
            .pdf-column
                div.pdf-list
                    .pdf-list-header(style=item.style)= item.value
                    div(style=options.columns.style)
                        each record in records ? records: []
                            div= record[columns.indexOf(item.name)]